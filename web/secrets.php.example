<?php
// Copy this file to secrets.php and fill in real values.
// secrets.php is git-ignored and must never be committed.

// Generate the password hash (run this on the server):
//   php -r "echo password_hash('YourStrongPassword', PASSWORD_DEFAULT);"
define('ADMIN_PASS_HASH', 'REPLACE_WITH_BCRYPT_HASH');

// Shared app secret — embedded in the Flutter app, used only to obtain a short-lived token.
//   php -r "echo bin2hex(random_bytes(32));"
define('APP_SECRET', 'REPLACE_WITH_LONG_RANDOM_KEY');

// Token signing key — SERVER ONLY, never sent to the app or committed to git.
//   php -r "echo bin2hex(random_bytes(32));"
define('TOKEN_SIGNING_KEY', 'REPLACE_WITH_ANOTHER_LONG_RANDOM_KEY');
